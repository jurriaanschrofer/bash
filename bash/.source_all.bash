#!/usr/bin/env bash

scripts_root=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

source $scripts_root/.a_huge_welcome.bash
source $scripts_root/.helpers.bash
source $scripts_root/.project_shortcuts.bash
source $scripts_root/.application_shortcuts.bash